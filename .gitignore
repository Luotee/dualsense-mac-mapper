# Superpowers brainstorming / plan artifacts — local working state only.
# Never tracked. Implementation lands in code + CHANGELOG, not in spec/plan
# markdown.
docs/superpowers/
.superpowers/

# Rust build output
**/target/

# IDE / OS noise
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*.swo

memory/

# Trace pipeline intermediate outputs (scripts/trace_*.py +
# gen_controller_constants.py write here). controller_geom.generated.js
# is the only artefact we track; the JSON inputs to it are local-only.
dist/

# Python script caches
scripts/__pycache__/
